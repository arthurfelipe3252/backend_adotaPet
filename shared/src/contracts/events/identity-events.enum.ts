export enum IdentityExchangeName {
  USER_CREATED = 'identity.users.created.exchange',
  USER_UPDATED = 'identity.users.updated.exchange',
  USER_DELETED = 'identity.users.deleted.exchange',
  ADOTANTE_CREATED = 'identity.adotantes.created.exchange',
  ADOTANTE_UPDATED = 'identity.adotantes.updated.exchange',
  PROTETOR_CREATED = 'identity.protetores.created.exchange',
  PROTETOR_UPDATED = 'identity.protetores.updated.exchange',
}

export enum IdentityRoutingKey {
  USER_CREATED = 'user.created',
  USER_UPDATED = 'user.updated',
  USER_DELETED = 'user.deleted',
  ADOTANTE_CREATED = 'adotante.created',
  ADOTANTE_UPDATED = 'adotante.updated',
  PROTETOR_CREATED = 'protetor.created',
  PROTETOR_UPDATED = 'protetor.updated',
}
