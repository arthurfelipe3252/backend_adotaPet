export enum AdoptionExchangeName {
  REQUEST_CREATED = 'adoption.requests.created.exchange',
  REQUEST_UPDATED = 'adoption.requests.updated.exchange',
}

export enum AdoptionRoutingKey {
  REQUEST_CREATED = 'adoption-request.created',
  REQUEST_UPDATED = 'adoption-request.updated',
}
